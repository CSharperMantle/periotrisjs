enum Direction {
  Left,
  Up,
  Right,
  Down,
}

enum RotationDirection {
  Left,
  Right,
}

enum MoveDirection {
  Left,
  Right,
  Down,
}

export { Direction, RotationDirection, MoveDirection }
